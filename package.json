{
  "name": "start-metal",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"vite\" \"python -m uvicorn main:app --app-dir api --port 3001 --reload\"",
    "dev:web": "vite",
    "dev:api": "python -m uvicorn main:app --app-dir api --port 3001 --reload",
    "build": "vite build",
    "start": "python -m uvicorn main:app --app-dir api --port 3001",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.1",
    "vite": "^5.4.8"
  }
}
